// A simple JavaScript test file

// Print a message
console.log("JavaScript test file is running!");

// A small function to add two numbers
function add(a, b) {
    return a + b;
}

// Test the function
console.log("2 + 3 =", add(2, 3));

// Export for testing frameworks (optional)
module.exports = { add };
